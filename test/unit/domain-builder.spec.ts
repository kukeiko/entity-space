import { DomainBuilder } from "@sandbox";

describe("domain-builder", () => {
    it("should do stuff", () => {
        let db = new DomainBuilder();

        expect(true).toBeTruthy();
    });
});
