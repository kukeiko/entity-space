import { getEntityMetadata, EntityMetadata, Primitive, Navigation, Reference, Collection, Workspace } from "../../src";
import { Album, Artist, Song, TagType } from "../common/entities";

describe("workspace", () => {
    it("foo", () => {
        // let ws = new Workspace();

        // let albums = ws.getMany({
        //     keys: [],
        //     type: Album
        // });

        // ws.add({
        //     type: Album,
        //     entity: {

        //     }
        // })
    });
});
