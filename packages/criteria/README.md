# criteria

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build criteria` to build the library.

## Running unit tests

Run `nx test criteria` to execute the unit tests via [Jest](https://jestjs.io).
